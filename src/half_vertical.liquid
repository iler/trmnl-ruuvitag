{%- comment -%}
  Left or right half of a side-by-side mashup.

  Below lg the half is about 390x480 and holds six compact cards. On TRMNL X
  it is roughly 520x780, which takes two columns of the full screen's cards —
  the compact version stretched to that width overflowed on both axes.
{%- endcomment -%}

{%- if fault != '' -%}
  {% render 'notice', headline: 'Ruuvi', detail: fault %}
{%- else -%}
  <div class="layout layout--col lg:hidden">
    <span class="title title--small">Indoor sensors</span>
    <div class="grid grid--cols-2 gap--small">
      {%- for sensor in featured limit: 6 -%}
        <div class="item{% if sensor.is_stale %} text--gray-30{% endif %}">
          <div class="flex flex--row gap--xsmall">
            <span class="label label--small stretch-x" data-clamp="1">{{ sensor.name }}</span>
            {% render 'sensor_flags', sensor: sensor %}
          </div>
          {%- if sensor.status == 'no_data' -%}
            <span class="value value--xsmall">no data</span>
          {%- else -%}
            <span class="value value--base value--tnums">{{ sensor.temperature }}&deg;C</span>
            <span class="label label--small">
              {{- sensor.humidity | default: '&mdash;' }}% &middot; {{ sensor.measured_at -}}
            </span>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>

  <div class="layout layout--col hidden lg:flex ruuvi">
    <div class="grid grid--cols-2 portrait:grid--cols-1 gap--small ruuvi__grid">
      {%- for sensor in featured limit: 6 -%}
        {%- assign rank_label = forloop.index | prepend: '0' -%}
        {% render 'sensor_card', sensor: sensor, rank: forloop.index, rank_label: rank_label %}
      {%- endfor -%}
    </div>
  </div>
{%- endif -%}

<div class="title_bar">
  <span class="title">Ruuvi</span>
  <span class="instance">{{ updated_at }}</span>
</div>
