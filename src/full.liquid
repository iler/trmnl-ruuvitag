{%- comment -%}
  Full screen: the top-priority sensor as a hero, the rest as an indexed
  ledger. The notice brings its own .layout — nesting one .layout inside
  another collapses it and the e-ink render comes back blank — so the fault
  branch sits outside the grid rather than inside it.
{%- endcomment -%}

{%- if fault != '' -%}
  {% render 'notice', headline: 'Ruuvi', detail: fault %}
{%- else -%}
  <div class="layout layout--col lg:hidden">
    <div class="grid grid--cols-12 gap--large stretch-y">

      <div class="col col--span-5">
        <div class="flex flex--col stretch-y gap--medium{% if primary.is_stale %} text--gray-30{% endif %}">
          <div class="flex flex--col gap--xsmall">
            <span class="label label--small label--underline">Station 01</span>
            <span class="title" data-clamp="1">{{ primary.name }}</span>
          </div>

          {%- if primary.status == 'no_data' -%}
            <span class="value value--xlarge">&mdash;</span>
            <span class="label label--small">awaiting reading</span>
          {%- else -%}
            <div class="flex flex--row gap--xsmall">
              <span class="value value--xlarge value--tnums">{{ primary.temperature }}</span>
              <span class="value value--base">&deg;C</span>
            </div>

            <div class="divider"></div>

            <div class="grid grid--cols-2 gap--medium">
              <div class="flex flex--col gap--xsmall">
                <span class="label label--small">Humidity</span>
                <span class="value value--base value--tnums">{{ primary.humidity | default: '&mdash;' }}%</span>
              </div>
              {%- comment -%}
                A Ruuvi Air swaps pressure for CO2, as its card does. Unlike
                the card, the hero labels each stat and carries a unit, so
                both are spelled out here — "1002 ppm" is no wider than the
                "1009 hPa" this slot already holds.
              {%- endcomment -%}
              {%- if primary.co2 != blank -%}
                <div class="flex flex--col gap--xsmall">
                  <span class="label label--small">CO&#8322;</span>
                  <span class="value value--base value--tnums">{{ primary.co2 }}<span class="value value--xsmall"> ppm</span></span>
                </div>
              {%- else -%}
                <div class="flex flex--col gap--xsmall">
                  <span class="label label--small">Pressure</span>
                  <span class="value value--base value--tnums">{{ primary.pressure_hpa | default: '&mdash;' }}<span class="value value--xsmall"> hPa</span></span>
                </div>
              {%- endif -%}
            </div>

            {%- comment -%} Pushes the footer line to the bottom of the column. {%- endcomment -%}
            <div class="stretch-y"></div>

            <div class="flex flex--row gap--xsmall">
              <span class="label label--small">
                {%- if primary.co2 != blank %}PM2.5 {{ primary.pm25 | default: '&mdash;' }} &middot; {% endif -%}
                read at {{ primary.measured_at }}
              </span>
              {% render 'sensor_flags', sensor: primary %}
            </div>
          {%- endif -%}
        </div>
      </div>

      <div class="col col--span-7">
        <table class="table table--small table--indexed ruuvi-ledger">
          <thead>
            <tr>
              <th class="pl--1"></th>
              <th class="pl--1"><span class="title title--small">Station</span></th>
              <th class="text--right pl--1"><span class="title title--small">&deg;C</span></th>
              <th class="text--right pl--1"><span class="title title--small">RH</span></th>
              <th class="text--right pl--1"><span class="title title--small">hPa</span></th>
              <th class="text--right pl--1"><span class="title title--small">Time</span></th>
            </tr>
          </thead>
          <tbody>
            {%- comment -%}
              offset: 1 — the hero already has the first one. The limit is what
              fits 800x480 without the table running off the bottom and shoving
              the hero off the top; anything past it is counted below.
            {%- endcomment -%}
            {%- for sensor in featured limit: 10 offset: 1 -%}
              <tr{% if sensor.is_stale %} class="text--gray-30"{% endif %}>
                <td>
                  <div class="meta">
                    <span class="index">{{ forloop.index | plus: 1 }}</span>
                  </div>
                </td>
                <td>
                  <div class="flex flex--row gap--xsmall">
                    <span data-clamp="1" class="label ruuvi-ledger__name">{{ sensor.name }}</span>
                    {% render 'sensor_flags', sensor: sensor %}
                  </div>
                </td>
                {%- if sensor.status == 'no_data' -%}
                  <td colspan="4" class="text--right pl--1"><span class="label label--small">no data</span></td>
                {%- else -%}
                  <td class="text--right pl--1"><span class="value value--small value--tnums">{{ sensor.temperature }}&deg;</span></td>
                  <td class="text--right pl--1"><span class="value value--small value--tnums">{{ sensor.humidity | default: '&mdash;' }}%</span></td>
                  <td class="text--right pl--1"><span class="value value--small value--tnums">{{ sensor.pressure_hpa | default: '&mdash;' }}</span></td>
                  <td class="text--right pl--1"><span class="label label--small">{{ sensor.measured_at }}</span></td>
                {%- endif -%}
              </tr>
            {%- else -%}
              <tr>
                <td colspan="6" class="text--center"><span class="label label--small">no additional stations</span></td>
              </tr>
            {%- endfor -%}
          </tbody>
        </table>
        {%- assign ledger_overflow = stats.total | minus: 11 -%}
        {%- if ledger_overflow > 0 -%}
          <span class="label label--small label--gray 1bit:text--black mt--1">
            {{- ledger_overflow }} more sensor{% if ledger_overflow > 1 %}s{% endif %} not shown
          </span>
        {%- endif -%}
      </div>

    </div>
  </div>

  {%- comment -%}
    TRMNL X and other large screens: the same sensors as cards, twelve of
    them, which is what a 4x3 grid holds at the canvas the device actually
    renders — 1872x1404 at scale_factor 1.8 is ~1040x780 in CSS pixels.
    Priority order decides which twelve, and anything past that is counted
    rather than dropped silently.

    This is a second .layout rather than a responsive variant of the first —
    a hero-plus-ledger and a card grid are different structures, not one
    structure at two sizes, and only one is ever displayed.
  {%- endcomment -%}
  {%- assign flagged_total = stats.alarms | plus: stats.low_battery | plus: stats.stale -%}
  {%- assign overflow_count = stats.total | minus: 12 -%}

  <div class="layout layout--col hidden lg:flex ruuvi">
    <header class="flex flex--row flex--between flex--bottom self--stretch pb--2 border--h-1 border--h-black ruuvi__header">
      <div class="flex flex--col gap--xsmall">
        <span class="title title--large">Indoor climate</span>
        <span class="label label--small label--gray 1bit:text--black">
          {{- stats.total }} stations &middot; {{ updated_at }} &middot; {{ time_zone_label -}}
        </span>
      </div>

      <div class="flex flex--row flex--center-y gap--small ruuvi__pills">
        {%- if stats.alarms > 0 -%}
          <span class="label label--error">{{ stats.alarms }} alert{% if stats.alarms > 1 %}s{% endif %}</span>
        {%- endif -%}
        {%- if stats.low_battery > 0 -%}
          <span class="label label--warning">{{ stats.low_battery }} low battery</span>
        {%- endif -%}
        {%- if stats.stale > 0 -%}
          <span class="label label--gray 1bit:text--black">{{ stats.stale }} stale</span>
        {%- endif -%}
        {%- if flagged_total == 0 and stats.total > 0 -%}
          <span class="label label--success">all nominal</span>
        {%- endif -%}
      </div>
    </header>

    <div class="grid grid--cols-4 portrait:grid--cols-3 gap--small ruuvi__grid">
      {%- for sensor in featured limit: 12 -%}
        {%- assign rank_label = forloop.index | prepend: '0' -%}
        {%- if forloop.index > 9 -%}{%- assign rank_label = forloop.index -%}{%- endif -%}
        {% render 'sensor_card', sensor: sensor, rank: forloop.index, rank_label: rank_label %}
      {%- endfor -%}
    </div>

    {%- if overflow_count > 0 -%}
      <span class="label label--small label--gray 1bit:text--black">
        {{- overflow_count }} more sensor{% if overflow_count > 1 %}s{% endif %} not shown &mdash; reorder them with the priority setting
      </span>
    {%- endif -%}
  </div>
{%- endif -%}

<div class="title_bar">
  <span class="title">Ruuvi &middot; Indoor climate</span>
  <span class="instance">
    {{- updated_at -}}
    {%- if unmatched.size > 0 %} &nbsp;&middot;&nbsp; not found: {{ unmatched | join: ', ' }}{% endif -%}
  </span>
</div>
