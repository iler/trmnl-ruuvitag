# trmnl-ruuvitag

A TRMNL plugin: Liquid templates plus a Serverless transform that decodes
RuuviTag BLE advertisements. See README.md for the layout and workflow.

## Agent skills

### Issue tracker

GitHub Issues on `iler/trmnl-ruuvitag`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
